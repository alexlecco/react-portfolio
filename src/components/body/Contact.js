import React from "react";

const GoalsContainer = ({ theme }) => {
  return (
    <div className={`${theme} h-64`}>
      <h1 className="text-center text-lg pt-8"> Contact me! </h1>
    </div>
  );
};

export default GoalsContainer;
